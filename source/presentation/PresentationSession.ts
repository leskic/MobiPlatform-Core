import type { PresentationSessionSnapshot } from "./interfaces/PresentationTypes";
export class PresentationSession { private id: string | undefined; create(id: string): PresentationSessionSnapshot { if (!id.trim()) throw new Error("Presentation session id cannot be empty"); this.id = id; return this.get(); } close(): PresentationSessionSnapshot { if (!this.id) throw new Error("No presentation session"); this.id = undefined; return this.get(); } require(): string { if (!this.id) throw new Error("No presentation session"); return this.id; } get(): PresentationSessionSnapshot { return { id: this.id ?? "", active: this.id !== undefined }; } }
