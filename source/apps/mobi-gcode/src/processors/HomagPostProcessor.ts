import type { ToolpathOperationSnapshot } from "../../../mobi-cam/src/interfaces/CAMTypes"; import { CommandGenerator } from "../generator/CommandGenerator"; import { BasePostProcessor } from "./BasePostProcessor";
export class HomagPostProcessor extends BasePostProcessor { readonly controller = "HOMAG" as const; protected commentPrefix(): string { return "//"; } protected begin(): string { return "BEGIN HOMAG"; } protected command(operation: ToolpathOperationSnapshot): string { const p = new CommandGenerator().coordinates(operation); return `HOP ${operation.type} X=${p.x} Y=${p.y} Z=${p.z} ID=${operation.id}`; } protected end(): string { return "END HOMAG"; } protected extension(): string { return "mpr"; } }
