import type { ToolpathOperationSnapshot } from "../../../mobi-cam/src/interfaces/CAMTypes"; import { CommandGenerator } from "../generator/CommandGenerator"; import { BasePostProcessor } from "./BasePostProcessor";
export class BiessePostProcessor extends BasePostProcessor { readonly controller = "BIESSE" as const; protected commentPrefix(): string { return "#"; } protected begin(): string { return "BEGIN BIESSE"; } protected command(operation: ToolpathOperationSnapshot): string { const p = new CommandGenerator().coordinates(operation); return `BPP:${operation.type};X=${p.x};Y=${p.y};Z=${p.z};ID=${operation.id}`; } protected end(): string { return "END BIESSE"; } protected extension(): string { return "bpp"; } }
