import type { ToolpathOperationSnapshot } from "../../../mobi-cam/src/interfaces/CAMTypes"; import { CommandGenerator } from "../generator/CommandGenerator"; import { BasePostProcessor } from "./BasePostProcessor";
export class MorbidelliPostProcessor extends BasePostProcessor { readonly controller = "MORBIDELLI" as const; protected commentPrefix(): string { return "'"; } protected begin(): string { return "MORBIDELLI BEGIN"; } protected command(operation: ToolpathOperationSnapshot): string { const p = new CommandGenerator().coordinates(operation); return `MORB ${operation.type} X${p.x} Y${p.y} Z${p.z} ${operation.id}`; } protected end(): string { return "MORBIDELLI END"; } protected extension(): string { return "xxl"; } }
