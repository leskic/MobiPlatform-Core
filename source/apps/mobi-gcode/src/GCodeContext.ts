import type { GCodeContextSnapshot, GCodeInput, GCodeMachineProfile, GCodePackageSnapshot } from "./interfaces/GCodeTypes";
export class GCodeContext { private readonly state: GCodeContextSnapshot; constructor(input: GCodeInput) { this.state = { input: structuredClone(input), profile: null, operations: [], output: null }; } setProfile(value: GCodeMachineProfile): void { this.state.profile = structuredClone(value); } setOperations(value: GCodeContextSnapshot["operations"]): void { this.state.operations = structuredClone(value); } setOutput(value: GCodePackageSnapshot): void { this.state.output = structuredClone(value); } get(): GCodeContextSnapshot { return structuredClone(this.state); } }
