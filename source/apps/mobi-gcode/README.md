# Mobi G-Code Foundation v1

Transforma exclusivamente um pacote CAM neutro em pacote de controlador validado. Exige perfil registrado, fingerprint atual, validação sintática e dry run. Não calcula geometria ou trajetórias e não altera Projeto.mobi.

Comandos: `npm run build`, `npm test` e `npm run coverage`.
