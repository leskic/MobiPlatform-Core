import { CabinetExtractor } from "../extraction/CabinetExtractor"; import { DinaboxExtractor } from "../extraction/DinaboxExtractor"; import { HardwareExtractor } from "../extraction/HardwareExtractor"; import { MaterialExtractor } from "../extraction/MaterialExtractor"; import { MetadataExtractor } from "../extraction/MetadataExtractor"; import { ModuleExtractor } from "../extraction/ModuleExtractor"; import { ParameterExtractor } from "../extraction/ParameterExtractor"; import type { DinaboxProjectInput, ExtractedDinaboxProject } from "../interfaces/DinaboxAdapterTypes";
export class ExtractionPipeline { private readonly extractor = new DinaboxExtractor(new ModuleExtractor(), new CabinetExtractor(), new HardwareExtractor(), new MaterialExtractor(), new ParameterExtractor(), new MetadataExtractor()); execute(project: DinaboxProjectInput): ExtractedDinaboxProject { return this.extractor.extract(project); } }
