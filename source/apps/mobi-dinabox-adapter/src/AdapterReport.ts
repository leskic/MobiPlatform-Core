import type { AdapterReportEntry, AdapterReportSnapshot, AdapterStatistics } from "./interfaces/DinaboxAdapterTypes";
export class AdapterReport { private readonly entries: AdapterReportEntry[] = []; private readonly startedAt: number; private finishedAt: number; private statistics: AdapterStatistics = { modules: 0, cabinets: 0, parts: 0, hardware: 0, materials: 0, imported: 0, ignored: 0, warnings: 0, errors: 0 }; constructor(private readonly now: () => number = () => 0) { this.startedAt = now(); this.finishedAt = this.startedAt; } setStatistics(values: Partial<AdapterStatistics>): void { this.statistics = { ...this.statistics, ...values }; } add(entry: Omit<AdapterReportEntry, "durationMs">, startedAt = this.now()): void { const end = this.now(); this.entries.push({ ...structuredClone(entry), durationMs: Math.max(0, end - startedAt) }); if (entry.severity === "warning") this.statistics.warnings += 1; if (entry.severity === "error") this.statistics.errors += 1; this.finishedAt = end; } get(): AdapterReportSnapshot { return structuredClone({ entries: this.entries, statistics: this.statistics, startedAt: this.startedAt, finishedAt: this.finishedAt, durationMs: Math.max(0, this.finishedAt - this.startedAt) }); } }
