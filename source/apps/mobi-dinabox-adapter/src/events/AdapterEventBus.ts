import type { AdapterEventSnapshot, AdapterListener } from "../interfaces/DinaboxAdapterTypes"; export class AdapterEventBus { private readonly listeners = new Set<AdapterListener>(); subscribe(listener: AdapterListener): void { this.listeners.add(listener); } unsubscribe(listener: AdapterListener): void { this.listeners.delete(listener); } publish(event: AdapterEventSnapshot): void { for (const listener of this.listeners) listener(structuredClone(event)); } clear(): void { this.listeners.clear(); } }
