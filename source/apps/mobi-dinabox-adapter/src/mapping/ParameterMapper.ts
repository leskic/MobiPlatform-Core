import type { ExtractedDinaboxProject, ParameterFact } from "../interfaces/DinaboxAdapterTypes";
export class ParameterMapper { map(input: ExtractedDinaboxProject): ParameterFact[] { return [{ ownerId: input.id, values: structuredClone(input.parameters) }, ...input.modules.map(item => ({ ownerId: item.id, values: structuredClone(item.parameters ?? {}) })), ...input.cabinets.map(item => ({ ownerId: item.id, values: structuredClone(item.parameters ?? {}) })), ...input.parts.map(item => ({ ownerId: item.id, values: structuredClone(item.parameters ?? {}) })), ...input.hardware.map(item => ({ ownerId: item.id, values: structuredClone(item.parameters ?? {}) }))]; } }
