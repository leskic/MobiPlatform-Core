# Mobi Constructor Foundation v1

Motor semântico headless para transformar descritores em um Projeto.mobi validado pelo Schema oficial e por todos os pacotes oficiais do RuleRunner.

O resultado aprovado contém uma `TransactionRequest` preparada, mas não escreve no Origin nem executa transações.

```bash
npm run build
npm test
npm run coverage
```
