import { RuleBook } from "../../../copilot/rulebook/RuleBook"; import { CabinetryRulePackage } from "../../../copilot/rulebook/packages/Cabinetry/CabinetryRulePackage"; import { CoreRulePackage } from "../../../copilot/rulebook/packages/Core/CoreRulePackage"; import { HardwareRulePackage } from "../../../copilot/rulebook/packages/Hardware/HardwareRulePackage"; import { ProductionRulePackage } from "../../../copilot/rulebook/packages/Production/ProductionRulePackage"; import { ConstructorContext } from "./ConstructorContext"; import { DomainMapper } from "./mapping/DomainMapper"; import { InputMapper } from "./mapping/InputMapper"; import { BuildStage } from "./pipeline/BuildStage"; import { CommitStage } from "./pipeline/CommitStage"; import { ConstructionPipeline } from "./pipeline/ConstructionPipeline"; import { ValidationStage } from "./pipeline/ValidationStage"; import { ConstructionValidator } from "./validation/ConstructionValidator"; import { RuleValidationStep } from "./validation/RuleValidationStep"; import { SchemaValidationStep } from "./validation/SchemaValidationStep"; import type { ConstructionInput, ConstructionStage, ConstructorResultSnapshot } from "./interfaces/ConstructorTypes";
export class ConstructorPipeline { private readonly pipeline: ConstructionPipeline; constructor(onStage: (stage: ConstructionStage) => void = () => {}) { const rulebook = new RuleBook(); const packages = [new CoreRulePackage(), new CabinetryRulePackage(), new HardwareRulePackage(), new ProductionRulePackage()]; for (const item of packages) { rulebook.registerPackage(item); rulebook.loadPackage(item.id); } const validator = new ConstructionValidator(new SchemaValidationStep(), new RuleValidationStep(rulebook.runner)); this.pipeline = new ConstructionPipeline(new InputMapper(), new BuildStage(new DomainMapper()), new ValidationStage(validator), new CommitStage(), onStage); } construct(input: ConstructionInput, now: () => number = () => 0): ConstructorResultSnapshot { return this.pipeline.run(new ConstructorContext(structuredClone(input), now)); } }
