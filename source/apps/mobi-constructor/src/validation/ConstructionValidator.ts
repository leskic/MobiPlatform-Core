import type { Project } from "../../../../builder/types/ProjectTypes"; import type { ConstructionValidation } from "../interfaces/ConstructorTypes"; import type { RuleValidationStep } from "./RuleValidationStep"; import type { SchemaValidationStep } from "./SchemaValidationStep";
export class ConstructionValidator { constructor(private readonly schema: SchemaValidationStep, private readonly rules: RuleValidationStep) {} validate(project: Project): ConstructionValidation { const schemaErrors = this.schema.validate(project); return { schemaErrors, ruleResults: this.rules.validate(project) }; } }
