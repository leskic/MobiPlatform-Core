import type{Project}from"../../../builder/types/ProjectTypes";import type{Diagnostic}from"../../mobi-cognitive-analyzer/src/DiagnosticTypes";import{PropositionCatalog}from"./PropositionCatalog";import{PropositionFactory}from"./PropositionFactory";import type{PropositionBatch}from"./PropositionTypes";import{PropositionValidator}from"./PropositionValidator";
export class PropositionEngine{constructor(private readonly factory:PropositionFactory,private readonly validator:PropositionValidator,private readonly catalog=new PropositionCatalog()){}propose(diagnostic:Diagnostic,project:Readonly<Project>,sequence:number):PropositionBatch{const propositions=[],blocked=[];for(const[ index,template]of this.catalog.templates(diagnostic).entries()){const proposal=this.factory.create(diagnostic,template,index,sequence),validation=this.validator.validate(proposal,diagnostic,project);if(validation.valid)propositions.push(proposal);else blocked.push(...validation.issues)}return{diagnostic:structuredClone(diagnostic),propositions:structuredClone(propositions),blocked:structuredClone(blocked)}}proposeAll(diagnostics:readonly Diagnostic[],project:Readonly<Project>,sequence:number){return diagnostics.map(d=>this.propose(d,project,sequence))}}

