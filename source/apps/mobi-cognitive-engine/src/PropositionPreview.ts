import type{PresentationCore}from"../../../presentation/PresentationCore";import type{PresentationSnapshot}from"../../../presentation/interfaces/PresentationTypes";import type{Proposition}from"./PropositionTypes";
export class PropositionPreview{private snapshot:PresentationSnapshot|undefined;private active:Proposition|undefined;constructor(private readonly presentation:PresentationCore){}start(proposition:Proposition,time:number){if(this.active)throw new Error("PREVIEW_ALREADY_ACTIVE");this.snapshot=this.presentation.getState();this.active=structuredClone(proposition);this.presentation.repository.selection.select(proposition.entityId);this.presentation.repository.view.set({mode:"inspection"});this.presentation.controller.notify("SelectionChanged",time);this.presentation.controller.notify("ViewChanged",time);return this.get()}cancel(time:number){if(!this.active||!this.snapshot)throw new Error("NO_ACTIVE_PREVIEW");this.presentation.repository.selection.selectMany(this.snapshot.selection.ids);this.presentation.repository.view.set(this.snapshot.view);this.presentation.controller.notify("SelectionChanged",time);this.presentation.controller.notify("ViewChanged",time);this.active=undefined;this.snapshot=undefined}get(){return this.active?{active:true as const,proposal:structuredClone(this.active)}:{active:false as const,proposal:null}}}

