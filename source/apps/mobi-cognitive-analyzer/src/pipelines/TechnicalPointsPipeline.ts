import{TechnicalPointValidator}from"../../../mobi-studio-technical-points/src/TechnicalPointValidator";import{diagnostic}from"../DiagnosticFactory";import type{DiagnosticPipeline}from"../DiagnosticTypes";export class TechnicalPointsPipeline implements DiagnosticPipeline{readonly id="technical-points";constructor(private readonly validator=new TechnicalPointValidator()){}run(p:Parameters<DiagnosticPipeline["run"]>[0],s:number){return p.environments.flatMap(e=>this.validator.validateEnvironment(e).issues.map(i=>diagnostic(this.id,i.code,"error",i.ids[0]??e.id,i.code,`/environments/${e.id}/infrastructures`,p.id,s)))}}
