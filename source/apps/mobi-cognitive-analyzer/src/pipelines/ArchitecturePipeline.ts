import type{DiagnosticPipeline}from"../DiagnosticTypes";import{diagnostic}from"../DiagnosticFactory";export class ArchitecturePipeline implements DiagnosticPipeline{readonly id="architecture";run(p:Parameters<DiagnosticPipeline["run"]>[0],s:number){const out= p.environments.filter(e=>e.architectures.length===0).map(e=>diagnostic(this.id,"ARCH_ENVIRONMENT_WITHOUT_ARCHITECTURE","warning",e.id,"Environment has no architectural context",`/environments/${e.id}/architectures`,p.id,s));return out}}
