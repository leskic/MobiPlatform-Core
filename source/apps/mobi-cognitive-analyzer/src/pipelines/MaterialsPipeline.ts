import type{DiagnosticPipeline}from"../DiagnosticTypes";import{diagnostic}from"../DiagnosticFactory";export class MaterialsPipeline implements DiagnosticPipeline{readonly id="materials";run(p:Parameters<DiagnosticPipeline["run"]>[0],s:number){return p.environments.flatMap(e=>e.modules.flatMap(m=>m.parts.filter(x=>!x.materialId.trim()).map(x=>diagnostic(this.id,"MATERIAL_MISSING_REFERENCE","error",x.id,"Part has no material reference",`/environments/${e.id}/modules/${m.id}/parts/${x.id}/materialId`,p.id,s))))}}
