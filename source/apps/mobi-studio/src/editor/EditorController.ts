import type { PresentationCore } from "../../../../presentation/PresentationCore";
import type { TransactionResultSnapshot } from "../../../../transaction/interfaces/TransactionTypes";
import type { TransactionCoordinator } from "../transactions/TransactionCoordinator";
import { EditIntent } from "./EditIntent"; import { EditPreview } from "./EditPreview"; import type { EditorState } from "./EditorState";
export class EditorController { private intent: EditIntent | undefined; private preview: EditPreview | undefined; constructor(private readonly presentation: PresentationCore, private readonly state: EditorState, private readonly transactions: TransactionCoordinator) {} begin(intent: EditIntent): void { if (this.intent) throw new Error("An edit is already active"); this.intent = intent; this.preview = new EditPreview(intent.input.preview); this.preview.start(this.presentation); this.state.set(intent.get()); } cancel(): void { this.preview?.cancel(this.presentation); this.intent = undefined; this.preview = undefined; this.state.clear(); } commit(): TransactionResultSnapshot { if (!this.intent || !this.preview) throw new Error("No active edit"); const result = this.transactions.execute(this.intent); if (result.success) this.preview.finish(); else this.preview.cancel(this.presentation); this.intent = undefined; this.preview = undefined; this.state.clear(); return result; } }
