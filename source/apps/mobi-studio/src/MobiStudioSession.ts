import type { MobiStudio } from "../../../studio/MobiStudio"; import { StudioSession } from "../../../studio/StudioSession";
export class MobiStudioSession { private applicationSession = new StudioSession(); constructor(private readonly foundation: MobiStudio) {} start(id: string): void { if (!id.trim()) throw new Error("Studio application session id cannot be empty"); this.applicationSession.create(id); this.foundation.createSession(id); } end(): void { if (!this.applicationSession.has()) throw new Error("No Studio application session"); this.applicationSession = new StudioSession(); } get(): { id: string; active: boolean } { return this.applicationSession.get(); } }
