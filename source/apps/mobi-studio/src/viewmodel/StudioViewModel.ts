import type { StudioRepository } from "../StudioRepository"; import type { ProjectTreeViewModel } from "./ProjectTreeViewModel"; import type { InspectorViewModel } from "./InspectorViewModel"; import type { TransactionViewModel } from "./TransactionViewModel"; import type { CopilotViewModel } from "./CopilotViewModel";
export class StudioViewModel { constructor(private readonly repository: StudioRepository, readonly tree: ProjectTreeViewModel, readonly inspector: InspectorViewModel, readonly transaction: TransactionViewModel, readonly copilot: CopilotViewModel) {} get() { return { studio: this.repository.get(), tree: this.tree.get(), inspector: this.inspector.get(), transaction: this.transaction.get(), copilot: this.copilot.get() }; } }
