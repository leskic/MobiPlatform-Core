import type { StudioShellSnapshot } from "../interfaces/StudioApplicationTypes"; import type { StudioLayout } from "./StudioLayout"; import type { StudioMenu } from "./StudioMenu"; import type { StudioToolbar } from "./StudioToolbar"; import type { ThemeManager } from "./ThemeManager";
export class StudioShell { constructor(private readonly menu: StudioMenu, private readonly toolbar: StudioToolbar, private readonly layout: StudioLayout, private readonly theme: ThemeManager) {} get(): StudioShellSnapshot { return { menu: this.menu.get(), toolbar: this.toolbar.get(), layout: this.layout.get(), theme: this.theme.get() }; } }
