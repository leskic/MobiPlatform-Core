import type { DockArea, DockSnapshot } from "../interfaces/StudioApplicationTypes";
export class DockManager { private readonly panels: Record<string, DockArea> = {}; private readonly visible: Record<string, boolean> = {}; dock(id: string, area: DockArea): void { this.panels[id] = area; this.visible[id] = true; } hide(id: string): void { if (this.panels[id] === undefined) throw new Error(`Dock panel not found: ${id}`); this.visible[id] = false; } show(id: string): void { if (this.panels[id] === undefined) throw new Error(`Dock panel not found: ${id}`); this.visible[id] = true; } remove(id: string): void { delete this.panels[id]; delete this.visible[id]; } get(): DockSnapshot { return structuredClone({ panels: this.panels, visible: this.visible }); } }
