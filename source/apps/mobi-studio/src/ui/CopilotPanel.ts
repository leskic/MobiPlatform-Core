import type { CopilotViewModel } from "../viewmodel/CopilotViewModel"; import type { PresentationCore } from "../../../../presentation/PresentationCore";
export class CopilotPanel { constructor(readonly viewModel: CopilotViewModel, private readonly presentation: PresentationCore) {} get() { return this.viewModel.get(); } selectRelated(index: number): void { const suggestion = this.viewModel.get().suggestions[index]; if (!suggestion) throw new Error("Suggestion not found"); const id = suggestion.path.split("/").filter(Boolean).at(-1); if (id) this.presentation.repository.selection.select(id); } }
