import type { CAMInput } from "../interfaces/CAMTypes"; import { FingerprintValidator } from "./FingerprintValidator"; export class CAMValidator { validate(input: CAMInput): { valid: boolean; code: string } { if (input.manufacturing.status === "REQUER_REVISAO") return { valid: false, code: "REVISION_REQUIRED" }; if (!new FingerprintValidator().validate(input)) return { valid: false, code: "FINGERPRINT_MISMATCH" }; return { valid: true, code: "CAM_READY" }; } }
