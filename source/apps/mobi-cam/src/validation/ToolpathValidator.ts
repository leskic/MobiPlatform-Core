import { CollisionAnalyzer } from "../geometry/CollisionAnalyzer"; import { MachineProfile } from "../MachineProfile"; import type { ToolpathPlanSnapshot } from "../interfaces/CAMTypes"; export class ToolpathValidator { validate(plan: ToolpathPlanSnapshot): { valid: boolean; errors: string[] } { const errors = new CollisionAnalyzer().analyze(plan.operations).map(id => `DUPLICATE_OPERATION:${id}`); for (const operation of plan.operations) { if (operation.depth <= 0) errors.push(`INVALID_DEPTH:${operation.id}`); if (operation.toolId === null) errors.push(`MISSING_TOOL:${operation.id}`); if (plan.machine && !new MachineProfile(plan.machine).supports(operation.type)) errors.push(`UNSUPPORTED_OPERATION:${operation.id}`); } return { valid: errors.length === 0, errors }; } }
