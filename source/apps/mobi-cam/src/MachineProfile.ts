import type { MachineProfileSnapshot, OperationType } from "./interfaces/CAMTypes"; export class MachineProfile { constructor(private value: MachineProfileSnapshot) { if (!value.id.trim() || value.maximumWidth <= 0 || value.maximumHeight <= 0) throw new Error("Invalid machine profile"); } supports(type: OperationType): boolean { return this.value.supportedOperations.includes(type); } get(): MachineProfileSnapshot { return structuredClone(this.value); } }
