import type { CAMEventSnapshot, CAMEventType } from "../interfaces/CAMTypes"; export class CAMEvent { constructor(private type: CAMEventType, private sessionId: string, private projectId: string, private logicalTimestamp: number) { if (!Number.isSafeInteger(logicalTimestamp) || logicalTimestamp < 0) throw new Error("CAM event timestamp must be non-negative"); } get(): CAMEventSnapshot { return { type: this.type, sessionId: this.sessionId, projectId: this.projectId, logicalTimestamp: this.logicalTimestamp }; } }
