import type { CAMEventSnapshot, CAMListener } from "../interfaces/CAMTypes"; export class CAMEventBus { private listeners = new Set<CAMListener>(); subscribe(value: CAMListener): void { this.listeners.add(value); } unsubscribe(value: CAMListener): void { this.listeners.delete(value); } publish(value: CAMEventSnapshot): void { for (const listener of this.listeners) listener(structuredClone(value)); } clear(): void { this.listeners.clear(); } }
