import { GeometryAnalyzer } from "../geometry/GeometryAnalyzer"; import { OperationSequence } from "../OperationSequence"; import type { CAMInput, ToolpathPlanSnapshot } from "../interfaces/CAMTypes"; import { OperationPlanner } from "./OperationPlanner"; import { OptimizationPlanner } from "./OptimizationPlanner"; import { ToolpathPlanner } from "./ToolpathPlanner";
export class CAMPlanner { plan(input: CAMInput): ToolpathPlanSnapshot { if (input.failPlanning) throw new Error("Requested CAM planning failure"); const facts = new GeometryAnalyzer().analyze(input.manufacturing.cuttingQueue); const operations = new OptimizationPlanner().plan(new ToolpathPlanner().assign(new OperationPlanner().plan(facts, input.explicitOperations ?? []), input.tools)); return { id: input.manufacturing.id, projectId: input.manufacturing.projectId, fingerprint: structuredClone(input.manufacturing.fingerprint), stage: "PLANNED", operations, sequence: new OperationSequence().create(operations), machine: structuredClone(input.machine ?? null) }; } }
