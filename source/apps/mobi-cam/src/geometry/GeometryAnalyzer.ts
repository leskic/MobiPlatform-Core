import type { PartListItem } from "../../../mobi-detalhamento/src/interfaces/DetailingTypes"; import type { GeometryFact } from "../interfaces/CAMTypes"; import { ContourBuilder } from "./ContourBuilder"; export class GeometryAnalyzer { analyze(parts: readonly PartListItem[]): GeometryFact[] { return parts.map(part => ({ part: structuredClone(part), contour: new ContourBuilder().rectangle(part.width, part.height) })); } }
