import type { NeutralToolpathSnapshot, ToolpathPlanSnapshot } from "../interfaces/CAMTypes"; export class NeutralToolpath { build(plan: ToolpathPlanSnapshot): NeutralToolpathSnapshot { if (plan.stage !== "VALIDATED") throw new Error("Toolpath must be validated before export"); return { format: "mobi-neutral-toolpath-v1", fingerprint: structuredClone(plan.fingerprint), plan: structuredClone(plan), machineCode: [] }; } }
