import type { CAMContextSnapshot, CAMInput, ToolpathOperationSnapshot, ToolpathPlanSnapshot } from "./interfaces/CAMTypes"; export class CAMContext { operations: ToolpathOperationSnapshot[] = []; plan: ToolpathPlanSnapshot | null = null; constructor(readonly input: CAMInput) {} get(): CAMContextSnapshot { return structuredClone({ input: this.input, operations: this.operations, plan: this.plan }); } }
