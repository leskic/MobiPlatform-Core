import type { Project } from "../../../builder/types/ProjectTypes"; import type { DivergenceReportSnapshot } from "../../../sync/interfaces/SyncTypes"; import type { SynchronizationContextSnapshot, SynchronizationInput } from "./interfaces/MobiDinaTypes";
export class SynchronizationContext { sourceProject: Project | null = null; destinationProject: Project | null = null; divergence: DivergenceReportSnapshot | null = null; constructor(readonly input: SynchronizationInput) { if (!input.author.trim()) throw new Error("Synchronization author cannot be empty"); } get(): SynchronizationContextSnapshot { return structuredClone({ input: this.input, sourceProject: this.sourceProject, destinationProject: this.destinationProject, divergence: this.divergence }); } }
