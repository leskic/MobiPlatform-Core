import {describe,expect,it} from "vitest"; import {MoveController} from "../src/MoveController"; import {ids,part,secondPartId,setup} from "./TestHarness";
describe("Move",()=>{it("moves multiple Parts, undo and redo",()=>{const x=setup();const tool=new MoveController(x.spatial);expect(tool.move([ids.part,secondPartId],{x:10,y:20,z:30},"a",1).success).toBe(true);expect(part(x.origin.getProject()).position).toEqual({x:10,y:20,z:30});expect(tool.undo().success).toBe(true);expect(part(x.origin.getProject()).position.x).toBe(0);expect(tool.redo().success).toBe(true);expect(part(x.origin.getProject()).position.x).toBe(10);});it("rolls back collision",()=>{const x=setup();const tool=new MoveController(x.spatial);const before=x.origin.getProject();const result=tool.move([secondPartId],{x:-2000,y:0,z:0},"a",2);expect(result.code).toBe("ROLLED_BACK");expect(x.origin.getProject()).toEqual(before);expect(result.transaction?.log.rollbackExecuted).toBe(true);});it("validates vectors and empty history",()=>{const x=setup();const tool=new MoveController(x.spatial);expect(()=>tool.move([ids.part],{x:NaN,y:0,z:0},"a",1)).toThrow("INVALID_VECTOR");expect(tool.undo().code).toBe("NOTHING_TO_UNDO");expect(tool.redo().code).toBe("NOTHING_TO_REDO");});});
