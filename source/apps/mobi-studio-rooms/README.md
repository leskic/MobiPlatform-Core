# Mobi Studio Rooms

Products Layer Fase 3.1. Rooms formalizam um `Environment` existente após validação determinística do loop de `Architecture(type="wall")`. A formalização é identificada pelo prefixo reservado da extensão `ROOM:` em `Environment.code`; nenhuma entidade ou campo de Schema foi criado.
