import type { MobiConstructor } from "../../../mobi-constructor/src/MobiConstructor"; import type { ConstructorResultSnapshot } from "../../../mobi-constructor/src/interfaces/ConstructorTypes"; import type { AdapterContext } from "../AdapterContext";
export class ConstructorPipeline { constructor(private readonly mobiConstructor: MobiConstructor) {} execute(context: AdapterContext): ConstructorResultSnapshot { if (!context.mapped) throw new Error("SketchUp input has not been mapped"); return this.mobiConstructor.construct({ descriptor: context.mapped.descriptor, transactionId: context.input.transactionId, logicalTimestamp: context.input.logicalTimestamp }, context.now); } }
