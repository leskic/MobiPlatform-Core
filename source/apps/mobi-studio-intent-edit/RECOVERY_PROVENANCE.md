# Recovery provenance

The original extension ZIP was not present in the downloaded Work backup. The four audited implementation files and two final reports were supplied directly in the conversation. `IntentEditTypes.ts`, `index.ts`, `manifest.json`, `README.md`, `tsconfig.json`, and `vitest.config.ts` were reconstructed to match the audited public contracts and source usage. No Foundation source was modified.
