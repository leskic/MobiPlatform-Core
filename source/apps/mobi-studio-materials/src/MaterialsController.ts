import type { Part,Project } from "../../../builder/types/ProjectTypes"; import {ParameterTransaction,type ParameterResult} from "./ParameterTransaction"; export class MaterialsController{constructor(private readonly tx:ParameterTransaction){} update(partIds:readonly string[],materialId:string,author:string,timestamp:number):ParameterResult{if(!materialId.trim())throw new Error("INCOMPATIBLE_MATERIAL");return this.tx.execute("UPDATE_PART_MATERIAL",partIds,project=>{for(const part of this.parts(project,partIds))part.materialId=materialId;},author,timestamp);}undo(){return this.tx.undo();}redo(){return this.tx.redo();}private parts(project:Project,ids:readonly string[]):Part[]{const found=project.environments.flatMap(e=>e.modules.flatMap(m=>m.parts)).filter(p=>ids.includes(p.id));if(found.length!==ids.length)throw new Error("ENTITY_NOT_PART");return found;}}
