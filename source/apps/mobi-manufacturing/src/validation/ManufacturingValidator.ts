import { validate } from "../../../../validator/SchemaValidator"; import { ProductionRulePackage } from "../../../../copilot/rulebook/packages/Production/ProductionRulePackage"; import { RuleRegistry } from "../../../../copilot/rules/RuleRegistry"; import { RuleRunner } from "../../../../copilot/rules/RuleRunner"; import { RuleContext } from "../../../../copilot/rules/RuleContext"; import type { ManufacturingInput } from "../interfaces/ManufacturingTypes"; import { FingerprintValidator } from "./FingerprintValidator";
export class ManufacturingValidator { validate(input: ManufacturingInput) { const schema = validate(input.project); const registry = new RuleRegistry(); for (const rule of new ProductionRulePackage().rules) registry.registerRule(rule); const production = [...new RuleRunner(registry).runAll(new RuleContext(input.project))]; const fingerprint = new FingerprintValidator().validate(input); return { valid: schema.valid && input.detailing.success && !input.detailing.obsolete && input.detailing.package !== undefined && fingerprint && !production.some(item => item.severity === "error"), schema, production, fingerprint }; } }
