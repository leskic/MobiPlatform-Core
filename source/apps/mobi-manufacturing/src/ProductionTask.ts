import type { ProductionTaskSnapshot, ProductionStatus } from "./interfaces/ManufacturingTypes"; export class ProductionTask { constructor(private value: ProductionTaskSnapshot) {} setStatus(status: ProductionStatus): void { this.value.status = status; } assign(workstation: string): void { if (!workstation.trim()) throw new Error("Workstation cannot be empty"); this.value.workstation = workstation; } get(): ProductionTaskSnapshot { return structuredClone(this.value); } }
