import type { ManufacturingSessionSnapshot } from "./interfaces/ManufacturingTypes"; export class ManufacturingSession { private id: string | undefined; create(id: string): ManufacturingSessionSnapshot { if (!id.trim()) throw new Error("Manufacturing session id cannot be empty"); this.id = id; return this.get(); } close(): ManufacturingSessionSnapshot { this.require(); this.id = undefined; return this.get(); } require(): string { if (!this.id) throw new Error("No active Manufacturing session"); return this.id; } get(): ManufacturingSessionSnapshot { return { id: this.id ?? "", active: this.id !== undefined }; } }
