import type { ProductionBatchSnapshot } from "../interfaces/ManufacturingTypes"; export class SequencePlanner { plan(input: readonly ProductionBatchSnapshot[]): ProductionBatchSnapshot[] { return structuredClone(input.map(batch => ({ ...batch, orders: batch.orders.map(order => ({ ...order, tasks: [...order.tasks] })) }))); } }
