import { DocumentFingerprint } from "../../mobi-detalhamento/src/DocumentFingerprint"; import type { Project } from "../../../builder/types/ProjectTypes"; import type { DocumentFingerprintSnapshot } from "../../mobi-detalhamento/src/interfaces/DetailingTypes"; export class FingerprintMonitor { current(project: Project, timestamp: number): DocumentFingerprintSnapshot { return new DocumentFingerprint().create(project, project.id, timestamp); } changed(project: Project, expected: DocumentFingerprintSnapshot, timestamp: number): boolean { return !new DocumentFingerprint().equals(this.current(project, timestamp), expected); } }
