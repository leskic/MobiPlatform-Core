import type { ManufacturingPlanSnapshot, ProductionStatus, TimelineEntry } from "./interfaces/ManufacturingTypes"; export class ManufacturingPlan { constructor(private value: ManufacturingPlanSnapshot) {} setStatus(status: ProductionStatus): void { this.value.status = status; } addTimeline(entry: TimelineEntry): void { this.value.timeline.push(structuredClone(entry)); } get(): ManufacturingPlanSnapshot { return structuredClone(this.value); } }
