import {describe,expect,it} from "vitest"; import {CopyController} from "../src/CopyController"; import {ids,part,setup} from "./TestHarness"; const newId="00000000-0000-4000-8000-000000000077";
describe("Copy",()=>{it("copies with explicit UUID, undo and redo",()=>{const x=setup();const tool=new CopyController(x.spatial);expect(tool.copy([{sourceId:ids.part,newId,offset:{x:1000,y:1000,z:0}}],"a",1).success).toBe(true);expect(part(x.origin.getProject(),newId).id).toBe(newId);tool.undo();expect(x.origin.getProject().environments[0]!.modules[0]!.parts.some(p=>p.id===newId)).toBe(false);tool.redo();expect(part(x.origin.getProject(),newId).id).toBe(newId);});it("rolls back colliding copy",()=>{const x=setup();expect(new CopyController(x.spatial).copy([{sourceId:ids.part,newId,offset:{x:0,y:0,z:0}}],"a",2).code).toBe("ROLLED_BACK");});it("validates descriptors",()=>{const x=setup();const tool=new CopyController(x.spatial);expect(()=>tool.copy([],"a",1)).toThrow("EMPTY_COPY");expect(()=>tool.copy([{sourceId:ids.part,newId,offset:{x:1,y:1,z:1}},{sourceId:ids.part,newId,offset:{x:2,y:2,z:2}}],"a",1)).toThrow("DUPLICATE_ID");});});
