import type{Camera,Scene}from"three";import{ProjectBuilder}from"../../../builder/ProjectBuilder";import{architectureInput,environmentInput,hardwareInput,ids,infrastructureInput,moduleInput,partInput,projectInput}from"../../../builder/tests/fixture";import{MobiCopilot}from"../../../copilot/MobiCopilot";import{RuleRegistry}from"../../../copilot/rules/RuleRegistry";import{RuleRunner}from"../../../copilot/rules/RuleRunner";import type{Rule}from"../../../copilot/rules/Rule";import{MobiOrigin}from"../../../origin/MobiOrigin";import{PresentationCore}from"../../../presentation/PresentationCore";import{MobiStudio}from"../../../studio/MobiStudio";import{SyncManager}from"../../../sync/SyncManager";import{Viewer}from"../../../viewer/Viewer";import type{FrameScheduler,RenderBackend}from"../../../viewer/interfaces/ViewerTypes";import{MobiStudioApplication}from"../../mobi-studio/src/MobiStudioApplication";class Backend implements RenderBackend{render(_s:Scene,_c:Camera):void{}setSize():void{}dispose():void{}}class Scheduler implements FrameScheduler{request():unknown{return 1}cancel():void{}}
export function setup(rules:readonly Rule[]){const origin=new MobiOrigin();const foundation=new MobiStudio(origin);const presentation=new PresentationCore();presentation.createSession("p");const app=new MobiStudioApplication({origin,foundation,presentation,viewer:new Viewer(presentation,new Backend(),new Scheduler()),copilot:new MobiCopilot(origin,foundation),sync:new SyncManager(origin)});app.startSession("s");const project=new ProjectBuilder().createProject(projectInput).addEnvironment(environmentInput).addArchitecture(ids.environment,architectureInput).addInfrastructure(ids.environment,infrastructureInput).addModule(ids.environment,moduleInput).addPart(ids.module,partInput).addHardware(ids.module,hardwareInput).build();foundation.openProject(JSON.stringify(project));const registry=new RuleRegistry();rules.forEach(rule=>registry.registerRule(rule));return{origin,foundation,presentation,app,runner:new RuleRunner(registry)};}export{ids,hardwareInput,partInput};
