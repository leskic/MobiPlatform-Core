import{describe,expect,it}from"vitest";import{RuleRegistry}from"../../../copilot/rules/RuleRegistry";import{RuleRunner}from"../../../copilot/rules/RuleRunner";import type{Rule}from"../../../copilot/rules/Rule";import*as api from"../src/index";import{setup}from"./TestHarness";
describe("thickness application contracts",()=>{it("exports APIs, validates empty selection and history",()=>{expect(api.ThicknessController).toBeDefined();const x=setup([]);const tx=new api.ParameterTransaction(x.app,x.presentation,x.runner);expect(()=>tx.execute("TEST",[],()=>{},"a",1)).toThrow("EMPTY_SELECTION");expect(tx.undo().code).toBe("NOTHING_TO_UNDO");expect(tx.redo().code).toBe("NOTHING_TO_REDO");expect(tx.project().id).toBeTruthy();});it("blocks concurrent parameter transactions",()=>{const x=setup([]);const registry=new RuleRegistry();let tx:api.ParameterTransaction;let nested="";const rule:Rule={id:"CONCURRENT",displayName:"c",description:"c",severity:"warning",enabled:true,analyze:()=>{nested=tx.execute("NESTED",["x"],()=>{},"a",2).code;return[];}};registry.registerRule(rule);tx=new api.ParameterTransaction(x.app,x.presentation,new RuleRunner(registry));expect(tx.execute("OUTER",["x"],()=>{},"a",1).success).toBe(true);expect(nested).toBe("EDIT_ACTIVE");});});
