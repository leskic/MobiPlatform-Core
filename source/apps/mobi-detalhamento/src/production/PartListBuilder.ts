import type { Project } from "../../../../builder/types/ProjectTypes"; import type { PartListItem } from "../interfaces/DetailingTypes"; export class PartListBuilder { build(project: Project): PartListItem[] { return project.environments.flatMap(e => e.modules.flatMap(m => m.parts.map(p => ({ id: p.id, materialId: p.materialId, width: p.size.width, height: p.size.height, thickness: p.size.thickness, quantity: 1 })))); } }
