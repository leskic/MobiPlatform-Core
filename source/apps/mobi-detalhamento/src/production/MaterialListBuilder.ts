import type { PartListItem, MaterialListItem } from "../interfaces/DetailingTypes"; export class MaterialListBuilder { build(parts: readonly PartListItem[]): MaterialListItem[] { const map = new Map<string, number>(); for (const part of parts) map.set(part.materialId, (map.get(part.materialId) ?? 0) + part.quantity); return [...map].map(([materialId, totalParts]) => ({ materialId, totalParts })); } }
