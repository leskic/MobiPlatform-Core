import type { Dimension } from "../interfaces/DetailingTypes"; export class DimensionBuilder { build(id: string, type: Dimension["type"], value: number, note?: string): Dimension { if (!Number.isFinite(value) || value < 0) throw new Error("Dimension must be non-negative"); const base = { id, type, value, unit: "mm" as const }; return note === undefined ? base : { ...base, note }; } }
