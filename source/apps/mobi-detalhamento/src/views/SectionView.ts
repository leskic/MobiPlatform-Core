import type { PresentationSnapshot } from "../../../../presentation/interfaces/PresentationTypes"; import type { TechnicalView } from "../interfaces/DetailingTypes"; import { createView } from "./FrontView"; export class SectionView { build(id: string, sectionId: string, state: PresentationSnapshot): TechnicalView { if (!sectionId.trim()) throw new Error("Section id cannot be empty"); return createView("SECTION", id, { ...structuredClone(state), view: { ...state.view, sections: true } }, sectionId); } }
