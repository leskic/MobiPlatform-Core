import type { PresentationSnapshot } from "../../../../presentation/interfaces/PresentationTypes"; import type { TechnicalView } from "../interfaces/DetailingTypes"; import { createView } from "./FrontView"; export class ExplodedView { build(id: string, state: PresentationSnapshot): TechnicalView { return createView("EXPLODED", id, { ...structuredClone(state), view: { ...state.view, exploded: true } }); } }
