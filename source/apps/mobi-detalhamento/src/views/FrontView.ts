import type { PresentationSnapshot } from "../../../../presentation/interfaces/PresentationTypes"; import type { TechnicalView, ViewKind } from "../interfaces/DetailingTypes"; export function createView(kind: ViewKind, id: string, presentation: PresentationSnapshot, sectionId?: string): TechnicalView { const value = { id, kind, presentation: structuredClone(presentation) }; return sectionId === undefined ? value : { ...value, sectionId }; } export class FrontView { build(id: string, state: PresentationSnapshot): TechnicalView { return createView("FRONT", id, state); } }
