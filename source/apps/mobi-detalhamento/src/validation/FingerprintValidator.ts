import { DocumentFingerprint } from "../DocumentFingerprint"; import type { DocumentFingerprintSnapshot } from "../interfaces/DetailingTypes"; export class FingerprintValidator { constructor(private service = new DocumentFingerprint()) {} isObsolete(existing: DocumentFingerprintSnapshot | undefined, current: DocumentFingerprintSnapshot): boolean { return existing !== undefined && !this.service.equals(existing, current); } }
