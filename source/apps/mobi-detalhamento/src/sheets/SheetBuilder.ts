import type { SheetLayoutSnapshot, TechnicalSheet } from "../interfaces/DetailingTypes"; export class SheetBuilder { build(id: string, number: number, title: string, revision: string, layout: SheetLayoutSnapshot): TechnicalSheet { if (!id.trim() || number < 1 || !revision.trim()) throw new Error("Invalid sheet identity"); return { id, number, title, revision, layout: structuredClone(layout), views: [], dimensions: [], annotations: [] }; } }
