import type { TechnicalSheet } from "../interfaces/DetailingTypes"; export class SheetCollection { private sheets: TechnicalSheet[] = []; add(sheet: TechnicalSheet): void { if (this.sheets.some(item => item.id === sheet.id || item.number === sheet.number)) throw new Error("Duplicate sheet"); this.sheets.push(structuredClone(sheet)); } get(): TechnicalSheet[] { return structuredClone([...this.sheets].sort((a, b) => a.number - b.number)); } clear(): void { this.sheets = []; } }
