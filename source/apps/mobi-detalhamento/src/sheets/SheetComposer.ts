import type { Annotation, Dimension, TechnicalSheet, TechnicalView } from "../interfaces/DetailingTypes"; export class SheetComposer { compose(sheet: TechnicalSheet, views: readonly TechnicalView[], dimensions: readonly Dimension[] = [], annotations: readonly Annotation[] = []): TechnicalSheet { return structuredClone({ ...sheet, views: [...views], dimensions: [...dimensions], annotations: [...annotations] }); } }
