import type { DocumentFingerprintSnapshot, TechnicalDocument, TechnicalSheet } from "../interfaces/DetailingTypes"; export class SvgDocument { build(projectId: string, timestamp: number, fingerprint: DocumentFingerprintSnapshot, sheets: readonly TechnicalSheet[], obsolete: boolean): TechnicalDocument { return { format: "SVG", projectId, version: "1.0.0", logicalTimestamp: timestamp, fingerprint: structuredClone(fingerprint), status: obsolete ? "OBSOLETE" : "CURRENT", sheets: [...structuredClone(sheets)], content: JSON.stringify({ mediaType: "image/svg+xml", projectId, sheets: sheets.map(s => s.id) }) }; } }
