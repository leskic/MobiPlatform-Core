import type { CuttingReport, DocumentFingerprintSnapshot, HardwareListItem, MaterialListItem, PartListItem, PrintPackageSnapshot, TechnicalDocument, Annotation } from "../interfaces/DetailingTypes"; export class PrintPackage { build(projectId: string, fingerprint: DocumentFingerprintSnapshot, pdf: TechnicalDocument, svg: TechnicalDocument, parts: PartListItem[], hardware: HardwareListItem[], materials: MaterialListItem[], cutting: CuttingReport, labels: Annotation[]): PrintPackageSnapshot { return structuredClone({ projectId, fingerprint, pdf, svg, parts, hardware, materials, cutting, labels }); } }
