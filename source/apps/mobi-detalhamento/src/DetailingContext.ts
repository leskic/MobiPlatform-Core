import type { DetailingContextSnapshot, DetailingInput, DocumentFingerprintSnapshot, TechnicalSheet } from "./interfaces/DetailingTypes"; export class DetailingContext { fingerprint: DocumentFingerprintSnapshot | null = null; sheets: TechnicalSheet[] = []; constructor(readonly input: DetailingInput) {} get(): DetailingContextSnapshot { return structuredClone({ input: this.input, fingerprint: this.fingerprint, sheets: this.sheets }); } }
