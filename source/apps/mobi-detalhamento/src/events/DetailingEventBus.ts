import type { DetailingEventSnapshot, DetailingListener } from "../interfaces/DetailingTypes"; export class DetailingEventBus { private listeners = new Set<DetailingListener>(); subscribe(value: DetailingListener): void { this.listeners.add(value); } unsubscribe(value: DetailingListener): void { this.listeners.delete(value); } publish(value: DetailingEventSnapshot): void { for (const listener of this.listeners) listener(structuredClone(value)); } clear(): void { this.listeners.clear(); } }
