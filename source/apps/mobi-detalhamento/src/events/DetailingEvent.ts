import type { DetailingEventSnapshot, DetailingEventType } from "../interfaces/DetailingTypes"; export class DetailingEvent { constructor(private type: DetailingEventType, private sessionId: string, private logicalTimestamp: number, private projectId: string) { if (!Number.isSafeInteger(logicalTimestamp) || logicalTimestamp < 0) throw new Error("Detailing event timestamp must be non-negative"); } get(): DetailingEventSnapshot { return { type: this.type, sessionId: this.sessionId, logicalTimestamp: this.logicalTimestamp, projectId: this.projectId }; } }
