import{describe,expect,it}from"vitest";import{InvalidGrainDirectionRule}from"../../../copilot/rulebook/packages/Production/InvalidGrainDirectionRule";import{GrainController}from"../src/GrainController";import{ids,setup}from"./TestHarness";
describe("Grain",()=>{it.each(["lengthwise","crosswise","none"]as const)("uses frozen Intent-Edit for %s",(value)=>{const x=setup([new InvalidGrainDirectionRule()]);const tool=new GrainController(x.app,x.presentation,x.runner);expect(tool.update(ids.part,value,"a",1).success).toBe(true);expect(x.origin.getProject().environments[0]!.modules[0]!.parts[0]!.grainDirection).toBe(value);});it("supports undo and redo through public Intent-Edit",()=>{const x=setup([new InvalidGrainDirectionRule()]);const tool=new GrainController(x.app,x.presentation,x.runner);tool.update(ids.part,"crosswise","a",1);expect(tool.undo().success).toBe(true);expect(tool.redo().success).toBe(true);expect(x.origin.getProject().environments[0]!.modules[0]!.parts[0]!.grainDirection).toBe("crosswise");});it("validates values and empty history",()=>{const x=setup([new InvalidGrainDirectionRule()]);const tool=new GrainController(x.app,x.presentation,x.runner);expect(()=>tool.update(ids.part,"diagonal"as never,"a",1)).toThrow("INVALID_GRAIN_DIRECTION");expect(tool.undo().code).toBe("NOTHING_TO_UNDO");expect(tool.redo().code).toBe("NOTHING_TO_REDO");});});
