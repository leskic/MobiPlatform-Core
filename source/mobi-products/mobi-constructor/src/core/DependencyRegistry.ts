export class DependencyRegistry{private readonly values=new Map<string,unknown>();register<T>(token:string,value:T):this{if(!token.trim()||value===undefined||this.values.has(token))throw new Error("INVALID_DEPENDENCY");this.values.set(token,value);return this}resolve<T>(token:string):T{if(!this.values.has(token))throw new Error(`MISSING_DEPENDENCY:${token}`);return this.values.get(token)as T}has(token:string){return this.values.has(token)}}
